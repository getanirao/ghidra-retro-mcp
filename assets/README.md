# Screenshots

Replace the placeholder `.png` files with actual screenshots:

1. **`demo-claude-triage.png`** — Claude Desktop conversation showing a ROM triage + decompile result
   - Configure Claude Desktop per the README
   - Ask: "Load this GBA ROM and decompile the entry point"
   - Screenshot the response showing decompiled C code

2. **`demo-terminal-triage.png`** — Terminal output from `triage_and_load_retro_rom`
   - Run: `ghidra-bizhawk-mcp` with a PSX EXE or NES ROM
   - Capture the session provisioned output showing platform, architecture, loader
